import { motion } from "framer-motion";
import React from "react";
type Props = {};

const Advantages = ({}: Props) => {
    return (
        <section className="h-[980px] py-20">
            <div></div>
        </section>
    );
};

export default Advantages;
